import 'package:flutter/material.dart';
import 'package:gameware/view/Login.dart';

final routes = {
  '/login': (BuildContext ctx) => new LoginView(),
  '/': (BuildContext ctx) => new LoginView(),
};